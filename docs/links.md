# Useful Links

* [Spark Pipelines API Reference](https://spark.apache.org/docs/latest/api/python/reference/pyspark.pipelines.html)

> Note: materialized_view in OSS SDP, currently supports `name: str`, `comment: str`, `spark_conf: Dict[str, str]`, `table_properties: Dict[str, str]`, `schema: StructType`, and `format: [delta, parquet, ...]`
